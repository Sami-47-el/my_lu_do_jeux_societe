{% extends 'base.html.twig' %}

{% block title %}
    Categories
{% endblock %}

{% block body %}
    <h1>
        Categories
    </h1>
    <table>
        <tr>
            <th>
                id
            </th>
            <td>
                {{ category.id }}
            </td>
        </tr>
        <tr>
        <th>
                name
            </th>
            <td>
                {{ category.name }}
            </td>
        </tr>
    </table>
    <a href="{{ path("app_category_index") }}">
        Retour à la liste
    </a>
    <a href="{{ path("app_category_edit", {'id':category.id}) }}">
         Modifier la liste
    </a>
     <a href="{{ path("app_category_delete", {'id':category.id}) }}">
         Supprimer la liste
    </a>
    {{include("category/delete/{id}")}}
{% endblock %}